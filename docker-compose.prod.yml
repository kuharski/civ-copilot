services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env.production

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:3000"
